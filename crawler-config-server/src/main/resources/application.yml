spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/crawler-plus/springcloud-config/
      label: master

server:
  port: 1003
  address: 127.0.0.1

management:
  health:
    defaults:
      enabled: true